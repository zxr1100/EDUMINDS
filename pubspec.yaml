name: vidyabridge_flutter
description: AI-Powered Vernacular STEM Learning Gap Analyzer
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Firebase
  #firebase_core: ^2.24.2
  #cloud_firestore: ^4.13.6
  #firebase_auth: ^4.15.3
  
  # State Management
  provider: ^6.1.1
  
  # UI & Animations
  google_fonts: ^6.1.0
  
  # HTTP requests
  http: ^1.1.2
  
  # Icons
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  
  # Assets
  assets:
    - assets/images/
    - assets/localization/

  # Fonts (optional, uncomment if needed)
  # fonts:
  #   - family: Poppins
  #     fonts:
  #       - asset: fonts/Poppins-Regular.ttf
  #       - asset: fonts/Poppins-Bold.ttf
  #         weight: 700